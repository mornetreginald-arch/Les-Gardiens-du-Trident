{% extends 'base.html.twig' %}

{% block title %}Chiots{% endblock %}

{% block body %}
    <h1>Chiots</h1>

    <table class="table">
        <tbody>
            
            <tr>
                <th>Sexe</th>
                <td>{{ chiot.sexe }}</td>
            </tr>
            <tr>
                <th>Couleur_collier</th>
                <td>{{ chiot.couleurCollier }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ chiot.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chiots_index') }}">back to list</a>

    <a href="{{ path('app_chiots_edit', {'id': chiot.id}) }}">edit</a>

    {{ include('chiots/_delete_form.html.twig') }}
{% endblock %}
