<form method="post" action="{{ path('app_chiots_delete', {'id': chiot.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chiot.id) }}">
    <button class="btn">Delete</button>
</form>
